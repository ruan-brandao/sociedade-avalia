<h1><%= @user.full_name %></h1>

<%= render 'stats' %>

<% if user_signed_in? && current_user != @user %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>

<%= render @posts %>
